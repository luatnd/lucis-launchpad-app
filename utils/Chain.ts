export enum Chain {
  eth = 'eth',
  bsc = 'bsc',
  polygon = 'polygon',
  near = 'near',
  avax = 'avax',
  flow = 'flow',
}

export enum Wallet {
  metamask = 'metamask',
  bsc = 'bsc',
  wc = 'wc', // wallet connect
}
