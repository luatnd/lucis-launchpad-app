overwrite: true
schema: "https://lucis-lp.koolab.io/graphql"
# documents: "src/**/*.graphql"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"

# -----> @graphql-codegen/introspection
#  ./graphql.schema.json:
#    plugins:
#      - "introspection"
